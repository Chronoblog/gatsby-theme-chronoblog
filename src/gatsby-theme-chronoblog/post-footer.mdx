import Gitalk from 'gatsby-plugin-gitalk'
import '@suziwen/gitalk/dist/gitalk.css'

<AuthorBanner></AuthorBanner>
<p>&nbsp;</p>
<Gitalk 
  options={{
    title: props.postData.frontmatter.title,
    id: props.postData.fields.slug,
    createIssueManually: true,
    number:3,
  }}
/>
